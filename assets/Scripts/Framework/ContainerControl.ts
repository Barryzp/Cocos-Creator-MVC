import BaseContentControl from "./BaseContentControl";

const { ccclass, property } = cc._decorator;
@ccclass
export default class ContainerControl extends BaseContentControl {
  onLoad() {
    super.onLoad();
  }
  start() {
    super.start();
  }
}